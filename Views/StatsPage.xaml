<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewmodels="clr-namespace:SoccerLinkPlayerSideApp.ViewModels"
             x:Class="SoccerLinkPlayerSideApp.Views.StatsPage"
             Title="{Binding Title}"
             BackgroundColor="#F0F2F5">

    <ScrollView>
        <Grid RowDefinitions="Auto, *" Padding="15" RowSpacing="20">

            <ActivityIndicator Grid.Row="0" IsRunning="{Binding IsBusy}" IsVisible="{Binding IsBusy}" Color="#2A5670" HorizontalOptions="Center"/>

            <VerticalStackLayout Grid.Row="1" Spacing="15" IsVisible="{Binding HasData}">

                <Border StrokeShape="RoundRectangle 15" BackgroundColor="#2A5670" StrokeThickness="0" Padding="20">
                    <Grid ColumnDefinitions="*, Auto, *" ColumnSpacing="10">
                        <VerticalStackLayout HorizontalOptions="Center">
                            <Label Text="{Binding MeczeRozegrane}" TextColor="White" FontSize="32" FontAttributes="Bold" HorizontalOptions="Center"/>
                            <Label Text="Mecze" TextColor="#D1D9E0" FontSize="14" HorizontalOptions="Center"/>
                        </VerticalStackLayout>

                        <BoxView Grid.Column="1" WidthRequest="1" Color="#5C7A8C" VerticalOptions="Fill"/>

                        <VerticalStackLayout Grid.Column="2" HorizontalOptions="Center">
                            <Label Text="{Binding TotalGole}" TextColor="White" FontSize="32" FontAttributes="Bold" HorizontalOptions="Center"/>
                            <Label Text="Gole" TextColor="#D1D9E0" FontSize="14" HorizontalOptions="Center"/>
                        </VerticalStackLayout>
                    </Grid>
                </Border>

                <Grid ColumnDefinitions="*,*" RowDefinitions="110, 110, 110" ColumnSpacing="15" RowSpacing="15">

                    <Border Grid.Row="0" Grid.Column="0" StrokeShape="RoundRectangle 15" BackgroundColor="White" StrokeThickness="0">
                        <VerticalStackLayout VerticalOptions="Center" HorizontalOptions="Center" Spacing="2">
                            <Label Text="🎯" FontSize="20"/>
                            <Label Text="{Binding TotalStrzaly}" TextColor="#2A5670" FontSize="24" FontAttributes="Bold"/>
                            <Label Text="Strzały" TextColor="Gray" FontSize="12"/>
                            <Label Text="{Binding TotalStrzalyCelne, StringFormat='(Celne: {0})'}" TextColor="Gray" FontSize="10"/>
                        </VerticalStackLayout>
                    </Border>

                    <Border Grid.Row="0" Grid.Column="1" StrokeShape="RoundRectangle 15" BackgroundColor="White" StrokeThickness="0">
                        <VerticalStackLayout VerticalOptions="Center" HorizontalOptions="Center" Spacing="2">
                            <Label Text="📈" FontSize="20"/>
                            <Label Text="{Binding Skutecznosc, StringFormat='{0:F0}%'}" TextColor="#2A5670" FontSize="24" FontAttributes="Bold"/>
                            <Label Text="Celność" TextColor="Gray" FontSize="12"/>
                        </VerticalStackLayout>
                    </Border>

                    <Border Grid.Row="1" Grid.Column="0" StrokeShape="RoundRectangle 15" BackgroundColor="White" StrokeThickness="0">
                        <VerticalStackLayout VerticalOptions="Center" HorizontalOptions="Center" Spacing="2">
                            <Label Text="🟨 🟥" FontSize="20"/>
                            <HorizontalStackLayout Spacing="5" HorizontalOptions="Center">
                                <Label Text="{Binding TotalZolteKartki}" TextColor="#F1C40F" FontSize="24" FontAttributes="Bold"/>
                                <Label Text="/" TextColor="Gray" FontSize="24"/>
                                <Label Text="{Binding TotalCzerwoneKartki}" TextColor="#E74C3C" FontSize="24" FontAttributes="Bold"/>
                            </HorizontalStackLayout>
                            <Label Text="Kartki" TextColor="Gray" FontSize="12"/>
                        </VerticalStackLayout>
                    </Border>

                    <Border Grid.Row="1" Grid.Column="1" StrokeShape="RoundRectangle 15" BackgroundColor="White" StrokeThickness="0">
                        <VerticalStackLayout VerticalOptions="Center" HorizontalOptions="Center" Spacing="2">
                            <Label Text="🧤" FontSize="20"/>
                            <Label Text="{Binding TotalCzysteKonta}" TextColor="#2A5670" FontSize="24" FontAttributes="Bold"/>
                            <Label Text="Czyste Konta" TextColor="Gray" FontSize="12"/>
                        </VerticalStackLayout>
                    </Border>

                    <Border Grid.Row="2" Grid.Column="0" StrokeShape="RoundRectangle 15" BackgroundColor="White" StrokeThickness="0">
                        <VerticalStackLayout VerticalOptions="Center" HorizontalOptions="Center" Spacing="2">
                            <Label Text="🛑" FontSize="20"/>
                            <Label Text="{Binding TotalFaule}" TextColor="#2A5670" FontSize="24" FontAttributes="Bold"/>
                            <Label Text="Faule" TextColor="Gray" FontSize="12"/>
                        </VerticalStackLayout>
                    </Border>

                    <Border Grid.Row="2" Grid.Column="1" StrokeShape="RoundRectangle 15" BackgroundColor="White" StrokeThickness="0">
                        <VerticalStackLayout VerticalOptions="Center" HorizontalOptions="Center" Spacing="2">
                            <Label Text="⚽/M" FontSize="20"/>
                            <Label Text="{Binding SredniaGoli, StringFormat='{0:F2}'}" TextColor="#2A5670" FontSize="24" FontAttributes="Bold"/>
                            <Label Text="Śr. Goli" TextColor="Gray" FontSize="12"/>
                        </VerticalStackLayout>
                    </Border>
                </Grid>
            </VerticalStackLayout>

            <VerticalStackLayout Grid.Row="1" Spacing="10" VerticalOptions="Center" HorizontalOptions="Center" 
                                 IsVisible="{Binding NoData}">
                <Label Text="🚫" FontSize="40" HorizontalOptions="Center"/>
                <Label Text="Brak danych statystycznych." TextColor="Black" FontSize="18" FontAttributes="Bold" HorizontalOptions="Center"/>
                <Label Text="Zagraj mecz, aby zobaczyć statystyki!" TextColor="Gray" FontSize="14" HorizontalOptions="Center"/>
            </VerticalStackLayout>

        </Grid>
    </ScrollView>
</ContentPage>