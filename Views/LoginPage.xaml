<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewmodels="clr-namespace:SoccerLinkPlayerSideApp.ViewModels"
             x:Class="SoccerLinkPlayerSideApp.Views.LoginPage"
             Title="Logowanie"
             Shell.NavBarIsVisible="False"
             BackgroundColor="White">

    <ScrollView>
        <VerticalStackLayout Spacing="20" Padding="30" VerticalOptions="Center">

            <Image Source="dotnet_bot.png" HeightRequest="150" HorizontalOptions="Center" />

            <Label Text="Witaj w SoccerLink!" FontSize="24" FontAttributes="Bold" HorizontalOptions="Center" TextColor="#2A5670" />

            <Entry Text="{Binding Email}" Placeholder="Email" Keyboard="Email" BackgroundColor="#F5F5F5" TextColor="Black" HeightRequest="50"/>

            <Entry Text="{Binding Password}" Placeholder="Hasło" IsPassword="True" BackgroundColor="#F5F5F5" TextColor="Black" HeightRequest="50"/>

            <Button Text="Zaloguj się" Command="{Binding LoginCommand}" BackgroundColor="#2A5670" TextColor="White" CornerRadius="8" HeightRequest="50" FontAttributes="Bold" Margin="0,20,0,0"/>

            <ActivityIndicator IsRunning="{Binding IsBusy}" IsVisible="{Binding IsBusy}" Color="#2A5670" HorizontalOptions="Center"/>

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>